/*
 * PayConductor API
 * # Introdução  Esta documentação cobre todas as funcionalidades disponíveis na API RESTful do PayConductor, incluindo autenticação, gerenciamento de recursos e exemplos de uso.  <br />  <br />  ----  <br />   # Autenticação  A API do PayConductor utiliza autenticação HTTP Basic para validar requisições. Você precisa fornecer suas credenciais (Client ID e Client Secret) no formato `client:secret` codificado em Base64.  <br />  ## Obtendo Credenciais  1. Acesse o painel administrativo do PayConductor 2. Navegue até **Configurações > API Keys** 3. Gere um novo par de credenciais (Client ID e Client Secret) 4. Guarde o Client Secret em local seguro - ele não será exibido novamente  <br />  ## Formato de Autenticação  As credenciais devem ser enviadas no header `Authorization` usando o esquema Basic:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Exemplo em Node.js  ```javascript const clientId = 'seu_client_id'; const clientSecret = 'seu_client_secret';  // Codifica as credenciais em Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Erros de Autenticação  | Código | Descrição | |--------|-----------| | `401`  | Credenciais inválidas ou ausentes | | `403`  | Credenciais válidas, mas sem permissão para o recurso | | `429`  | Muitas requisições (rate limit excedido) |  ### Exemplo de resposta de erro  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.CartOTokenizado;
import org.openapitools.client.model.DadosCompletosDoCartO;
import org.openapitools.client.model.DadosCompletosDoCartOExpiration;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-02-18T14:44:19.268927700-03:00[America/Bahia]", comments = "Generator version: 7.20.0")
public class CartODeCrDitoCard extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CartODeCrDitoCard.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CartODeCrDitoCard.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CartODeCrDitoCard' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<CartOTokenizado> adapterCartOTokenizado = gson.getDelegateAdapter(this, TypeToken.get(CartOTokenizado.class));
            final TypeAdapter<DadosCompletosDoCartO> adapterDadosCompletosDoCartO = gson.getDelegateAdapter(this, TypeToken.get(DadosCompletosDoCartO.class));

            return (TypeAdapter<T>) new TypeAdapter<CartODeCrDitoCard>() {
                @Override
                public void write(JsonWriter out, CartODeCrDitoCard value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `CartOTokenizado`
                    if (value.getActualInstance() instanceof CartOTokenizado) {
                        JsonElement element = adapterCartOTokenizado.toJsonTree((CartOTokenizado)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `DadosCompletosDoCartO`
                    if (value.getActualInstance() instanceof DadosCompletosDoCartO) {
                        JsonElement element = adapterDadosCompletosDoCartO.toJsonTree((DadosCompletosDoCartO)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match anyOf schemas: CartOTokenizado, DadosCompletosDoCartO");
                }

                @Override
                public CartODeCrDitoCard read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize CartOTokenizado
                    try {
                        // validate the JSON object to see if any exception is thrown
                        CartOTokenizado.validateJsonElement(jsonElement);
                        actualAdapter = adapterCartOTokenizado;
                        CartODeCrDitoCard ret = new CartODeCrDitoCard();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for CartOTokenizado failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'CartOTokenizado'", e);
                    }
                    // deserialize DadosCompletosDoCartO
                    try {
                        // validate the JSON object to see if any exception is thrown
                        DadosCompletosDoCartO.validateJsonElement(jsonElement);
                        actualAdapter = adapterDadosCompletosDoCartO;
                        CartODeCrDitoCard ret = new CartODeCrDitoCard();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for DadosCompletosDoCartO failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'DadosCompletosDoCartO'", e);
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for CartODeCrDitoCard: no class matches result, expected at least 1. Detailed failure message for anyOf schemas: %s. JSON: %s", errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in anyOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CartODeCrDitoCard() {
        super("anyOf", Boolean.FALSE);
    }

    public CartODeCrDitoCard(Object o) {
        super("anyOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("CartOTokenizado", CartOTokenizado.class);
        schemas.put("DadosCompletosDoCartO", DadosCompletosDoCartO.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CartODeCrDitoCard.schemas;
    }

    /**
     * Set the instance that matches the anyOf child schema, check
     * the instance parameter is valid against the anyOf child schemas:
     * CartOTokenizado, DadosCompletosDoCartO
     *
     * It could be an instance of the 'anyOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof CartOTokenizado) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof DadosCompletosDoCartO) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be CartOTokenizado, DadosCompletosDoCartO");
    }

    /**
     * Get the actual instance, which can be the following:
     * CartOTokenizado, DadosCompletosDoCartO
     *
     * @return The actual instance (CartOTokenizado, DadosCompletosDoCartO)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `CartOTokenizado`. If the actual instance is not `CartOTokenizado`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `CartOTokenizado`
     * @throws ClassCastException if the instance is not `CartOTokenizado`
     */
    public CartOTokenizado getCartOTokenizado() throws ClassCastException {
        return (CartOTokenizado)super.getActualInstance();
    }

    /**
     * Get the actual instance of `DadosCompletosDoCartO`. If the actual instance is not `DadosCompletosDoCartO`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `DadosCompletosDoCartO`
     * @throws ClassCastException if the instance is not `DadosCompletosDoCartO`
     */
    public DadosCompletosDoCartO getDadosCompletosDoCartO() throws ClassCastException {
        return (DadosCompletosDoCartO)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CartODeCrDitoCard
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate anyOf schemas one by one
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with CartOTokenizado
        try {
            CartOTokenizado.validateJsonElement(jsonElement);
            return;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for CartOTokenizado failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with DadosCompletosDoCartO
        try {
            DadosCompletosDoCartO.validateJsonElement(jsonElement);
            return;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for DadosCompletosDoCartO failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for CartODeCrDitoCard with anyOf schemas: CartOTokenizado, DadosCompletosDoCartO. no class match the result, expected at least 1. Detailed failure message for anyOf schemas: %s. JSON: %s", errorMessages, jsonElement.toString()));
    }

    /**
     * Create an instance of CartODeCrDitoCard given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CartODeCrDitoCard
     * @throws IOException if the JSON string is invalid with respect to CartODeCrDitoCard
     */
    public static CartODeCrDitoCard fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CartODeCrDitoCard.class);
    }

    /**
     * Convert an instance of CartODeCrDitoCard to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

