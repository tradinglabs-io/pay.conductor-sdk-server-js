/**
 * PayConductor API
 * # Introdução  Esta documentação cobre todas as funcionalidades disponíveis na API RESTful do PayConductor, incluindo autenticação, gerenciamento de recursos e exemplos de uso.  <br />  <br />  ----  <br />   # Autenticação  A API do PayConductor utiliza autenticação HTTP Basic para validar requisições. Você precisa fornecer suas credenciais (Client ID e Client Secret) no formato `client:secret` codificado em Base64.  <br />  ## Obtendo Credenciais  1. Acesse o painel administrativo do PayConductor 2. Navegue até **Configurações > API Keys** 3. Gere um novo par de credenciais (Client ID e Client Secret) 4. Guarde o Client Secret em local seguro - ele não será exibido novamente  <br />  ## Formato de Autenticação  As credenciais devem ser enviadas no header `Authorization` usando o esquema Basic:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Exemplo em Node.js  ```javascript const clientId = 'seu_client_id'; const clientSecret = 'seu_client_secret';  // Codifica as credenciais em Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Erros de Autenticação  | Código | Descrição | |--------|-----------| | `401`  | Credenciais inválidas ou ausentes | | `403`  | Credenciais válidas, mas sem permissão para o recurso | | `429`  | Muitas requisições (rate limit excedido) |  ### Exemplo de resposta de erro  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DadosCompletosDoCartOExpiration from './DadosCompletosDoCartOExpiration';

/**
 * The DadosCompletosDoCartO model module.
 * @module model/DadosCompletosDoCartO
 * @version 1.0.0
 */
class DadosCompletosDoCartO {
    /**
     * Constructs a new <code>DadosCompletosDoCartO</code>.
     * @alias module:model/DadosCompletosDoCartO
     * @param cvv {String} Código de segurança do cartão (CVV)
     * @param expiration {module:model/DadosCompletosDoCartOExpiration} 
     * @param holderName {String} Nome do titular do cartão
     * @param number {String} Número do cartão de crédito
     */
    constructor(cvv, expiration, holderName, number) { 
        
        DadosCompletosDoCartO.initialize(this, cvv, expiration, holderName, number);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cvv, expiration, holderName, number) { 
        obj['cvv'] = cvv;
        obj['expiration'] = expiration;
        obj['holderName'] = holderName;
        obj['number'] = number;
    }

    /**
     * Constructs a <code>DadosCompletosDoCartO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DadosCompletosDoCartO} obj Optional instance to populate.
     * @return {module:model/DadosCompletosDoCartO} The populated <code>DadosCompletosDoCartO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DadosCompletosDoCartO();

            if (data.hasOwnProperty('cvv')) {
                obj['cvv'] = ApiClient.convertToType(data['cvv'], 'String');
            }
            if (data.hasOwnProperty('expiration')) {
                obj['expiration'] = DadosCompletosDoCartOExpiration.constructFromObject(data['expiration']);
            }
            if (data.hasOwnProperty('holderName')) {
                obj['holderName'] = ApiClient.convertToType(data['holderName'], 'String');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DadosCompletosDoCartO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DadosCompletosDoCartO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DadosCompletosDoCartO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cvv'] && !(typeof data['cvv'] === 'string' || data['cvv'] instanceof String)) {
            throw new Error("Expected the field `cvv` to be a primitive type in the JSON string but got " + data['cvv']);
        }
        // validate the optional field `expiration`
        if (data['expiration']) { // data not null
          DadosCompletosDoCartOExpiration.validateJSON(data['expiration']);
        }
        // ensure the json data is a string
        if (data['holderName'] && !(typeof data['holderName'] === 'string' || data['holderName'] instanceof String)) {
            throw new Error("Expected the field `holderName` to be a primitive type in the JSON string but got " + data['holderName']);
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }

        return true;
    }


}

DadosCompletosDoCartO.RequiredProperties = ["cvv", "expiration", "holderName", "number"];

/**
 * Código de segurança do cartão (CVV)
 * @member {String} cvv
 */
DadosCompletosDoCartO.prototype['cvv'] = undefined;

/**
 * @member {module:model/DadosCompletosDoCartOExpiration} expiration
 */
DadosCompletosDoCartO.prototype['expiration'] = undefined;

/**
 * Nome do titular do cartão
 * @member {String} holderName
 */
DadosCompletosDoCartO.prototype['holderName'] = undefined;

/**
 * Número do cartão de crédito
 * @member {String} number
 */
DadosCompletosDoCartO.prototype['number'] = undefined;






export default DadosCompletosDoCartO;

