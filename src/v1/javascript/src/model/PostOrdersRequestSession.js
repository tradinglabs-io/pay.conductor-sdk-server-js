/**
 * PayConductor API
 * # Introdução  Esta documentação cobre todas as funcionalidades disponíveis na API RESTful do PayConductor, incluindo autenticação, gerenciamento de recursos e exemplos de uso.  <br />  <br />  ----  <br />   # Autenticação  A API do PayConductor utiliza autenticação HTTP Basic para validar requisições. Você precisa fornecer suas credenciais (Client ID e Client Secret) no formato `client:secret` codificado em Base64.  <br />  ## Obtendo Credenciais  1. Acesse o painel administrativo do PayConductor 2. Navegue até **Configurações > API Keys** 3. Gere um novo par de credenciais (Client ID e Client Secret) 4. Guarde o Client Secret em local seguro - ele não será exibido novamente  <br />  ## Formato de Autenticação  As credenciais devem ser enviadas no header `Authorization` usando o esquema Basic:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Exemplo em Node.js  ```javascript const clientId = 'seu_client_id'; const clientSecret = 'seu_client_secret';  // Codifica as credenciais em Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Erros de Autenticação  | Código | Descrição | |--------|-----------| | `401`  | Credenciais inválidas ou ausentes | | `403`  | Credenciais válidas, mas sem permissão para o recurso | | `429`  | Muitas requisições (rate limit excedido) |  ### Exemplo de resposta de erro  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostOrdersRequestSession model module.
 * @module model/PostOrdersRequestSession
 * @version 1.0.0
 */
class PostOrdersRequestSession {
    /**
     * Constructs a new <code>PostOrdersRequestSession</code>.
     * Caso o externalSessionId ou sessionId sejam fornecidos e houver uma sessão existente, ela será atualizada com os novos dados. Caso contrário, uma nova sessão será criada.
     * @alias module:model/PostOrdersRequestSession
     */
    constructor() { 
        
        PostOrdersRequestSession.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['expiresInMinutes'] = 30;
    }

    /**
     * Constructs a <code>PostOrdersRequestSession</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostOrdersRequestSession} obj Optional instance to populate.
     * @return {module:model/PostOrdersRequestSession} The populated <code>PostOrdersRequestSession</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostOrdersRequestSession();

            if (data.hasOwnProperty('expiresInMinutes')) {
                obj['expiresInMinutes'] = ApiClient.convertToType(data['expiresInMinutes'], 'Number');
            }
            if (data.hasOwnProperty('checkoutUrl')) {
                obj['checkoutUrl'] = ApiClient.convertToType(data['checkoutUrl'], 'String');
            }
            if (data.hasOwnProperty('sessionId')) {
                obj['sessionId'] = ApiClient.convertToType(data['sessionId'], 'String');
            }
            if (data.hasOwnProperty('externalSessionId')) {
                obj['externalSessionId'] = ApiClient.convertToType(data['externalSessionId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostOrdersRequestSession</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostOrdersRequestSession</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['checkoutUrl'] && !(typeof data['checkoutUrl'] === 'string' || data['checkoutUrl'] instanceof String)) {
            throw new Error("Expected the field `checkoutUrl` to be a primitive type in the JSON string but got " + data['checkoutUrl']);
        }
        // ensure the json data is a string
        if (data['sessionId'] && !(typeof data['sessionId'] === 'string' || data['sessionId'] instanceof String)) {
            throw new Error("Expected the field `sessionId` to be a primitive type in the JSON string but got " + data['sessionId']);
        }
        // ensure the json data is a string
        if (data['externalSessionId'] && !(typeof data['externalSessionId'] === 'string' || data['externalSessionId'] instanceof String)) {
            throw new Error("Expected the field `externalSessionId` to be a primitive type in the JSON string but got " + data['externalSessionId']);
        }

        return true;
    }


}



/**
 * Tempo em minutos para expiração da sessão do checkout
 * @member {Number} expiresInMinutes
 * @default 30
 */
PostOrdersRequestSession.prototype['expiresInMinutes'] = 30;

/**
 * URL do checkout para que possamos redirecionar
 * @member {String} checkoutUrl
 */
PostOrdersRequestSession.prototype['checkoutUrl'] = undefined;

/**
 * Id gerado pelo nosso serviço para identificar a sessão de checkout
 * @member {String} sessionId
 */
PostOrdersRequestSession.prototype['sessionId'] = undefined;

/**
 * Id da sessão de checkout gerado pelo lado do cliente. O id deve ser único para cada sessão de carrinho
 * @member {String} externalSessionId
 */
PostOrdersRequestSession.prototype['externalSessionId'] = undefined;






export default PostOrdersRequestSession;

