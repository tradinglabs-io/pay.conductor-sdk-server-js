/**
 * PayConductor API
 * PayConductor API Documentation.  This documentation covers all available features in the PayConductor RESTful API, including authentication, resource management, and usage examples.  <br />  <br />  ----  <br />   # Authentication  PayConductor API uses HTTP Basic authentication to validate requests. You need to provide your credentials (Client ID and Client Secret) in the `client:secret` format encoded in Base64.  <br />  ## Getting Credentials  1. Access the PayConductor admin panel 2. Navigate to **Settings > API Keys** 3. Generate a new credentials pair (Client ID and Client Secret) 4. Store the Client Secret in a secure location - it will not be displayed again  <br />  ## Authentication Format  Credentials must be sent in the `Authorization` header using the Basic scheme:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Node.js Example  ```javascript const clientId = 'your_client_id'; const clientSecret = 'your_client_secret';  // Encode credentials in Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Authentication Errors  | Code | Description | |------|-------------| | `401` | Invalid or missing credentials | | `403` | Valid credentials but no permission for the resource | | `429` | Too many requests (rate limit exceeded) |  ### Error Response Example  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CompleteCardData from './CompleteCardData';
import PostCardTokenizationRequestCustomer from './PostCardTokenizationRequestCustomer';

/**
 * The PostCardTokenizationRequest model module.
 * @module model/PostCardTokenizationRequest
 * @version 1.0.0
 */
class PostCardTokenizationRequest {
    /**
     * Constructs a new <code>PostCardTokenizationRequest</code>.
     * Data for creating a customer card
     * @alias module:model/PostCardTokenizationRequest
     * @param card {module:model/CompleteCardData} 
     * @param saveCard {Boolean} Indicates if the card should be saved for future use
     * @param customer {module:model/PostCardTokenizationRequestCustomer} 
     */
    constructor(card, saveCard, customer) { 
        
        PostCardTokenizationRequest.initialize(this, card, saveCard, customer);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, card, saveCard, customer) { 
        obj['card'] = card;
        obj['saveCard'] = saveCard;
        obj['customer'] = customer;
    }

    /**
     * Constructs a <code>PostCardTokenizationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostCardTokenizationRequest} obj Optional instance to populate.
     * @return {module:model/PostCardTokenizationRequest} The populated <code>PostCardTokenizationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostCardTokenizationRequest();

            if (data.hasOwnProperty('card')) {
                obj['card'] = CompleteCardData.constructFromObject(data['card']);
            }
            if (data.hasOwnProperty('saveCard')) {
                obj['saveCard'] = ApiClient.convertToType(data['saveCard'], 'Boolean');
            }
            if (data.hasOwnProperty('customer')) {
                obj['customer'] = PostCardTokenizationRequestCustomer.constructFromObject(data['customer']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostCardTokenizationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostCardTokenizationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostCardTokenizationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `card`
        if (data['card']) { // data not null
          CompleteCardData.validateJSON(data['card']);
        }
        // validate the optional field `customer`
        if (data['customer']) { // data not null
          PostCardTokenizationRequestCustomer.validateJSON(data['customer']);
        }

        return true;
    }


}

PostCardTokenizationRequest.RequiredProperties = ["card", "saveCard", "customer"];

/**
 * @member {module:model/CompleteCardData} card
 */
PostCardTokenizationRequest.prototype['card'] = undefined;

/**
 * Indicates if the card should be saved for future use
 * @member {Boolean} saveCard
 */
PostCardTokenizationRequest.prototype['saveCard'] = undefined;

/**
 * @member {module:model/PostCardTokenizationRequestCustomer} customer
 */
PostCardTokenizationRequest.prototype['customer'] = undefined;






export default PostCardTokenizationRequest;

