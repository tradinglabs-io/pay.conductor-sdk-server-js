/**
 * PayConductor API
 * PayConductor API Documentation.  This documentation covers all available features in the PayConductor RESTful API, including authentication, resource management, and usage examples.  <br />  <br />  ----  <br />   # Authentication  PayConductor API uses HTTP Basic authentication to validate requests. You need to provide your credentials (Client ID and Client Secret) in the `client:secret` format encoded in Base64.  <br />  ## Getting Credentials  1. Access the PayConductor admin panel 2. Navigate to **Settings > API Keys** 3. Generate a new credentials pair (Client ID and Client Secret) 4. Store the Client Secret in a secure location - it will not be displayed again  <br />  ## Authentication Format  Credentials must be sent in the `Authorization` header using the Basic scheme:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Node.js Example  ```javascript const clientId = 'your_client_id'; const clientSecret = 'your_client_secret';  // Encode credentials in Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Authentication Errors  | Code | Description | |------|-------------| | `401` | Invalid or missing credentials | | `403` | Valid credentials but no permission for the resource | | `429` | Too many requests (rate limit exceeded) |  ### Error Response Example  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CompleteCardDataExpiration from './CompleteCardDataExpiration';

/**
 * The CompleteCardData model module.
 * @module model/CompleteCardData
 * @version 1.0.0
 */
class CompleteCardData {
    /**
     * Constructs a new <code>CompleteCardData</code>.
     * @alias module:model/CompleteCardData
     * @param cvv {String} Card security code (CVV)
     * @param expiration {module:model/CompleteCardDataExpiration} 
     * @param holderName {String} Card holder name
     * @param number {String} Credit card number
     */
    constructor(cvv, expiration, holderName, number) { 
        
        CompleteCardData.initialize(this, cvv, expiration, holderName, number);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cvv, expiration, holderName, number) { 
        obj['cvv'] = cvv;
        obj['expiration'] = expiration;
        obj['holderName'] = holderName;
        obj['number'] = number;
    }

    /**
     * Constructs a <code>CompleteCardData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CompleteCardData} obj Optional instance to populate.
     * @return {module:model/CompleteCardData} The populated <code>CompleteCardData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CompleteCardData();

            if (data.hasOwnProperty('cvv')) {
                obj['cvv'] = ApiClient.convertToType(data['cvv'], 'String');
            }
            if (data.hasOwnProperty('expiration')) {
                obj['expiration'] = CompleteCardDataExpiration.constructFromObject(data['expiration']);
            }
            if (data.hasOwnProperty('holderName')) {
                obj['holderName'] = ApiClient.convertToType(data['holderName'], 'String');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CompleteCardData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CompleteCardData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CompleteCardData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cvv'] && !(typeof data['cvv'] === 'string' || data['cvv'] instanceof String)) {
            throw new Error("Expected the field `cvv` to be a primitive type in the JSON string but got " + data['cvv']);
        }
        // validate the optional field `expiration`
        if (data['expiration']) { // data not null
          CompleteCardDataExpiration.validateJSON(data['expiration']);
        }
        // ensure the json data is a string
        if (data['holderName'] && !(typeof data['holderName'] === 'string' || data['holderName'] instanceof String)) {
            throw new Error("Expected the field `holderName` to be a primitive type in the JSON string but got " + data['holderName']);
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }

        return true;
    }


}

CompleteCardData.RequiredProperties = ["cvv", "expiration", "holderName", "number"];

/**
 * Card security code (CVV)
 * @member {String} cvv
 */
CompleteCardData.prototype['cvv'] = undefined;

/**
 * @member {module:model/CompleteCardDataExpiration} expiration
 */
CompleteCardData.prototype['expiration'] = undefined;

/**
 * Card holder name
 * @member {String} holderName
 */
CompleteCardData.prototype['holderName'] = undefined;

/**
 * Credit card number
 * @member {String} number
 */
CompleteCardData.prototype['number'] = undefined;






export default CompleteCardData;

