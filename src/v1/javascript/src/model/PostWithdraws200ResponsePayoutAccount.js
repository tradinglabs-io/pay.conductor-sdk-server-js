/**
 * PayConductor API
 * PayConductor API Documentation.  This documentation covers all available features in the PayConductor RESTful API, including authentication, resource management, and usage examples.  <br />  <br />  ----  <br />   # Authentication  PayConductor API uses HTTP Basic authentication to validate requests. You need to provide your credentials (Client ID and Client Secret) in the `client:secret` format encoded in Base64.  <br />  ## Getting Credentials  1. Access the PayConductor admin panel 2. Navigate to **Settings > API Keys** 3. Generate a new credentials pair (Client ID and Client Secret) 4. Store the Client Secret in a secure location - it will not be displayed again  <br />  ## Authentication Format  Credentials must be sent in the `Authorization` header using the Basic scheme:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Node.js Example  ```javascript const clientId = 'your_client_id'; const clientSecret = 'your_client_secret';  // Encode credentials in Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Authentication Errors  | Code | Description | |------|-------------| | `401` | Invalid or missing credentials | | `403` | Valid credentials but no permission for the resource | | `429` | Too many requests (rate limit exceeded) |  ### Error Response Example  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostWithdraws200ResponsePayoutAccount model module.
 * @module model/PostWithdraws200ResponsePayoutAccount
 * @version 1.0.0
 */
class PostWithdraws200ResponsePayoutAccount {
    /**
     * Constructs a new <code>PostWithdraws200ResponsePayoutAccount</code>.
     * @alias module:model/PostWithdraws200ResponsePayoutAccount
     * @param id {String} Unique payment account identifier
     * @param ownerDocument {String} Account holder document (CPF or CNPJ)
     * @param ownerName {String} Account holder name
     * @param pixKey {String} PIX key used for the withdrawal
     * @param pixType {module:model/PostWithdraws200ResponsePayoutAccount.PixTypeEnum} PIX key type
     */
    constructor(id, ownerDocument, ownerName, pixKey, pixType) { 
        
        PostWithdraws200ResponsePayoutAccount.initialize(this, id, ownerDocument, ownerName, pixKey, pixType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, ownerDocument, ownerName, pixKey, pixType) { 
        obj['id'] = id;
        obj['ownerDocument'] = ownerDocument;
        obj['ownerName'] = ownerName;
        obj['pixKey'] = pixKey;
        obj['pixType'] = pixType || 'Cpf';
    }

    /**
     * Constructs a <code>PostWithdraws200ResponsePayoutAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostWithdraws200ResponsePayoutAccount} obj Optional instance to populate.
     * @return {module:model/PostWithdraws200ResponsePayoutAccount} The populated <code>PostWithdraws200ResponsePayoutAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostWithdraws200ResponsePayoutAccount();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('ownerDocument')) {
                obj['ownerDocument'] = ApiClient.convertToType(data['ownerDocument'], 'String');
            }
            if (data.hasOwnProperty('ownerName')) {
                obj['ownerName'] = ApiClient.convertToType(data['ownerName'], 'String');
            }
            if (data.hasOwnProperty('pixKey')) {
                obj['pixKey'] = ApiClient.convertToType(data['pixKey'], 'String');
            }
            if (data.hasOwnProperty('pixType')) {
                obj['pixType'] = ApiClient.convertToType(data['pixType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostWithdraws200ResponsePayoutAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostWithdraws200ResponsePayoutAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostWithdraws200ResponsePayoutAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['ownerDocument'] && !(typeof data['ownerDocument'] === 'string' || data['ownerDocument'] instanceof String)) {
            throw new Error("Expected the field `ownerDocument` to be a primitive type in the JSON string but got " + data['ownerDocument']);
        }
        // ensure the json data is a string
        if (data['ownerName'] && !(typeof data['ownerName'] === 'string' || data['ownerName'] instanceof String)) {
            throw new Error("Expected the field `ownerName` to be a primitive type in the JSON string but got " + data['ownerName']);
        }
        // ensure the json data is a string
        if (data['pixKey'] && !(typeof data['pixKey'] === 'string' || data['pixKey'] instanceof String)) {
            throw new Error("Expected the field `pixKey` to be a primitive type in the JSON string but got " + data['pixKey']);
        }
        // ensure the json data is a string
        if (data['pixType'] && !(typeof data['pixType'] === 'string' || data['pixType'] instanceof String)) {
            throw new Error("Expected the field `pixType` to be a primitive type in the JSON string but got " + data['pixType']);
        }

        return true;
    }


}

PostWithdraws200ResponsePayoutAccount.RequiredProperties = ["id", "ownerDocument", "ownerName", "pixKey", "pixType"];

/**
 * Unique payment account identifier
 * @member {String} id
 */
PostWithdraws200ResponsePayoutAccount.prototype['id'] = undefined;

/**
 * Account holder document (CPF or CNPJ)
 * @member {String} ownerDocument
 */
PostWithdraws200ResponsePayoutAccount.prototype['ownerDocument'] = undefined;

/**
 * Account holder name
 * @member {String} ownerName
 */
PostWithdraws200ResponsePayoutAccount.prototype['ownerName'] = undefined;

/**
 * PIX key used for the withdrawal
 * @member {String} pixKey
 */
PostWithdraws200ResponsePayoutAccount.prototype['pixKey'] = undefined;

/**
 * PIX key type
 * @member {module:model/PostWithdraws200ResponsePayoutAccount.PixTypeEnum} pixType
 * @default 'Cpf'
 */
PostWithdraws200ResponsePayoutAccount.prototype['pixType'] = 'Cpf';





/**
 * Allowed values for the <code>pixType</code> property.
 * @enum {String}
 * @readonly
 */
PostWithdraws200ResponsePayoutAccount['PixTypeEnum'] = {

    /**
     * value: "Cpf"
     * @const
     */
    "Cpf": "Cpf",

    /**
     * value: "Cnpj"
     * @const
     */
    "Cnpj": "Cnpj",

    /**
     * value: "Email"
     * @const
     */
    "Email": "Email",

    /**
     * value: "Phone"
     * @const
     */
    "Phone": "Phone",

    /**
     * value: "Random"
     * @const
     */
    "Random": "Random"
};



export default PostWithdraws200ResponsePayoutAccount;

