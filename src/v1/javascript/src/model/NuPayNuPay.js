/**
 * PayConductor API
 * # Introdução  Esta documentação cobre todas as funcionalidades disponíveis na API RESTful do PayConductor, incluindo autenticação, gerenciamento de recursos e exemplos de uso.  <br />  <br />  ----  <br />   # Autenticação  A API do PayConductor utiliza autenticação HTTP Basic para validar requisições. Você precisa fornecer suas credenciais (Client ID e Client Secret) no formato `client:secret` codificado em Base64.  <br />  ## Obtendo Credenciais  1. Acesse o painel administrativo do PayConductor 2. Navegue até **Configurações > API Keys** 3. Gere um novo par de credenciais (Client ID e Client Secret) 4. Guarde o Client Secret em local seguro - ele não será exibido novamente  <br />  ## Formato de Autenticação  As credenciais devem ser enviadas no header `Authorization` usando o esquema Basic:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Exemplo em Node.js  ```javascript const clientId = 'seu_client_id'; const clientSecret = 'seu_client_secret';  // Codifica as credenciais em Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Erros de Autenticação  | Código | Descrição | |--------|-----------| | `401`  | Credenciais inválidas ou ausentes | | `403`  | Credenciais válidas, mas sem permissão para o recurso | | `429`  | Muitas requisições (rate limit excedido) |  ### Exemplo de resposta de erro  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NuPayNuPay model module.
 * @module model/NuPayNuPay
 * @version 1.0.0
 */
class NuPayNuPay {
    /**
     * Constructs a new <code>NuPayNuPay</code>.
     * Dados específicos para pagamento via NuPay
     * @alias module:model/NuPayNuPay
     * @param cancelUrl {String} URL de cancelamento do pagamento
     * @param merchantName {String} Nome do comerciante
     * @param returnUrl {String} URL de retorno após a conclusão do pagamento
     */
    constructor(cancelUrl, merchantName, returnUrl) { 
        
        NuPayNuPay.initialize(this, cancelUrl, merchantName, returnUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cancelUrl, merchantName, returnUrl) { 
        obj['cancelUrl'] = cancelUrl;
        obj['merchantName'] = merchantName;
        obj['returnUrl'] = returnUrl;
    }

    /**
     * Constructs a <code>NuPayNuPay</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NuPayNuPay} obj Optional instance to populate.
     * @return {module:model/NuPayNuPay} The populated <code>NuPayNuPay</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NuPayNuPay();

            if (data.hasOwnProperty('cancelUrl')) {
                obj['cancelUrl'] = ApiClient.convertToType(data['cancelUrl'], 'String');
            }
            if (data.hasOwnProperty('merchantName')) {
                obj['merchantName'] = ApiClient.convertToType(data['merchantName'], 'String');
            }
            if (data.hasOwnProperty('returnUrl')) {
                obj['returnUrl'] = ApiClient.convertToType(data['returnUrl'], 'String');
            }
            if (data.hasOwnProperty('storeName')) {
                obj['storeName'] = ApiClient.convertToType(data['storeName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NuPayNuPay</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NuPayNuPay</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NuPayNuPay.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cancelUrl'] && !(typeof data['cancelUrl'] === 'string' || data['cancelUrl'] instanceof String)) {
            throw new Error("Expected the field `cancelUrl` to be a primitive type in the JSON string but got " + data['cancelUrl']);
        }
        // ensure the json data is a string
        if (data['merchantName'] && !(typeof data['merchantName'] === 'string' || data['merchantName'] instanceof String)) {
            throw new Error("Expected the field `merchantName` to be a primitive type in the JSON string but got " + data['merchantName']);
        }
        // ensure the json data is a string
        if (data['returnUrl'] && !(typeof data['returnUrl'] === 'string' || data['returnUrl'] instanceof String)) {
            throw new Error("Expected the field `returnUrl` to be a primitive type in the JSON string but got " + data['returnUrl']);
        }
        // ensure the json data is a string
        if (data['storeName'] && !(typeof data['storeName'] === 'string' || data['storeName'] instanceof String)) {
            throw new Error("Expected the field `storeName` to be a primitive type in the JSON string but got " + data['storeName']);
        }

        return true;
    }


}

NuPayNuPay.RequiredProperties = ["cancelUrl", "merchantName", "returnUrl"];

/**
 * URL de cancelamento do pagamento
 * @member {String} cancelUrl
 */
NuPayNuPay.prototype['cancelUrl'] = undefined;

/**
 * Nome do comerciante
 * @member {String} merchantName
 */
NuPayNuPay.prototype['merchantName'] = undefined;

/**
 * URL de retorno após a conclusão do pagamento
 * @member {String} returnUrl
 */
NuPayNuPay.prototype['returnUrl'] = undefined;

/**
 * Nome da loja
 * @member {String} storeName
 */
NuPayNuPay.prototype['storeName'] = undefined;






export default NuPayNuPay;

