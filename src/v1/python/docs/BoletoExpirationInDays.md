# BoletoExpirationInDays

Dias até o vencimento do boleto

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from payconductor-sdk.models.boleto_expiration_in_days import BoletoExpirationInDays

# TODO update the JSON string below
json = "{}"
# create an instance of BoletoExpirationInDays from a JSON string
boleto_expiration_in_days_instance = BoletoExpirationInDays.from_json(json)
# print the JSON string representation of the object
print(BoletoExpirationInDays.to_json())

# convert the object into a dict
boleto_expiration_in_days_dict = boleto_expiration_in_days_instance.to_dict()
# create an instance of BoletoExpirationInDays from a dict
boleto_expiration_in_days_from_dict = BoletoExpirationInDays.from_dict(boleto_expiration_in_days_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


