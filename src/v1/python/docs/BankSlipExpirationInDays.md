# BankSlipExpirationInDays

Days until bank slip expires

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from payconductor-sdk.models.bank_slip_expiration_in_days import BankSlipExpirationInDays

# TODO update the JSON string below
json = "{}"
# create an instance of BankSlipExpirationInDays from a JSON string
bank_slip_expiration_in_days_instance = BankSlipExpirationInDays.from_json(json)
# print the JSON string representation of the object
print(BankSlipExpirationInDays.to_json())

# convert the object into a dict
bank_slip_expiration_in_days_dict = bank_slip_expiration_in_days_instance.to_dict()
# create an instance of BankSlipExpirationInDays from a dict
bank_slip_expiration_in_days_from_dict = BankSlipExpirationInDays.from_dict(bank_slip_expiration_in_days_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


