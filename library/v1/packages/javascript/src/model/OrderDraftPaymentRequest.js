/**
 * PayConductor API
 * PayConductor API Documentation.  This documentation covers all available features in the PayConductor RESTful API, including authentication, resource management, and usage examples.  <br />  <br />  ----  <br />   # Authentication  PayConductor API uses HTTP Basic authentication to validate requests. You need to provide your credentials (Client ID and Client Secret) in the `client:secret` format encoded in Base64.  <br />  ## Getting Credentials  1. Access the PayConductor admin panel 2. Navigate to **Settings > API Keys** 3. Generate a new credentials pair (Client ID and Client Secret) 4. Store the Client Secret in a secure location - it will not be displayed again  <br />  ## Authentication Format  Credentials must be sent in the `Authorization` header using the Basic scheme:  ``` Authorization: Basic base64(client_id:client_secret) ```  <br />  ## Node.js Example  ```javascript const clientId = 'your_client_id'; const clientSecret = 'your_client_secret';  // Encode credentials in Base64 const credentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');  const response = await fetch('https://api.payconductor.com/api/v1/orders', {   method: 'GET',   headers: {     'Authorization': `Basic ${credentials}`,     'Content-Type': 'application/json'   } });  const data = await response.json(); console.log(data); ```  <br />  ## Authentication Errors  | Code | Description | |------|-------------| | `401` | Invalid or missing credentials | | `403` | Valid credentials but no permission for the resource | | `429` | Too many requests (rate limit exceeded) |  ### Error Response Example  ```json {   \"error\": {     \"code\": \"UNAUTHORIZED\",     \"message\": \"Invalid credentials\",     \"details\": \"The provided client ID or secret is incorrect\"   } } ```
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AvailablePaymentMethods from './AvailablePaymentMethods';
import PaymentMethod from './PaymentMethod';

/**
 * The OrderDraftPaymentRequest model module.
 * @module model/OrderDraftPaymentRequest
 * @version 1.0.0
 */
class OrderDraftPaymentRequest {
    /**
     * Constructs a new <code>OrderDraftPaymentRequest</code>.
     * Used to create an order without generating a real payment. Use to create orders that will be paid later
     * @alias module:model/OrderDraftPaymentRequest
     * @param paymentMethod {module:model/PaymentMethod} 
     */
    constructor(paymentMethod) { 
        
        OrderDraftPaymentRequest.initialize(this, paymentMethod);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, paymentMethod) { 
        obj['paymentMethod'] = paymentMethod;
        obj['expirationInSeconds'] = 3600;
    }

    /**
     * Constructs a <code>OrderDraftPaymentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderDraftPaymentRequest} obj Optional instance to populate.
     * @return {module:model/OrderDraftPaymentRequest} The populated <code>OrderDraftPaymentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderDraftPaymentRequest();

            if (data.hasOwnProperty('paymentMethod')) {
                obj['paymentMethod'] = PaymentMethod.constructFromObject(data['paymentMethod']);
            }
            if (data.hasOwnProperty('expirationInSeconds')) {
                obj['expirationInSeconds'] = ApiClient.convertToType(data['expirationInSeconds'], 'Number');
            }
            if (data.hasOwnProperty('availablePaymentMethods')) {
                obj['availablePaymentMethods'] = ApiClient.convertToType(data['availablePaymentMethods'], [AvailablePaymentMethods]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderDraftPaymentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderDraftPaymentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrderDraftPaymentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['availablePaymentMethods'])) {
            throw new Error("Expected the field `availablePaymentMethods` to be an array in the JSON data but got " + data['availablePaymentMethods']);
        }

        return true;
    }


}

OrderDraftPaymentRequest.RequiredProperties = ["paymentMethod"];

/**
 * @member {module:model/PaymentMethod} paymentMethod
 */
OrderDraftPaymentRequest.prototype['paymentMethod'] = undefined;

/**
 * Order expiration time in seconds
 * @member {Number} expirationInSeconds
 * @default 3600
 */
OrderDraftPaymentRequest.prototype['expirationInSeconds'] = 3600;

/**
 * Available payment methods for this order
 * @member {Array.<module:model/AvailablePaymentMethods>} availablePaymentMethods
 */
OrderDraftPaymentRequest.prototype['availablePaymentMethods'] = undefined;






export default OrderDraftPaymentRequest;

